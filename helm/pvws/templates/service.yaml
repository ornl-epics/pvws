apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-service"
  namespace: {{ .Release.Namespace }}
  labels:
    app: pvws
spec:
  type: ClusterIP
  ports:
    - name: graphql
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: pvws